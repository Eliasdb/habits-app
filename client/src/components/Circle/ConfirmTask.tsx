const ConfirmTask = () => {
  return (
    <button
      type="button"
      className="confirm-btn"
      onClick={() => console.log("click")}
    >
      Add task
    </button>
  );
};
export default ConfirmTask;
